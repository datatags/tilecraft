name: Test Action

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Run a multi-line script
      run: |
        echo Zipping...
        zip tilecraft.zip project.json assets/*
        pwd
        mv tilecraft.zip tilecraft.sb3
